// Mechanical ESM build of the canonical server engine. Do not hand-edit the
// generated browser module: both sides must use exactly the same tennis rules.
const fs = require('node:fs')
const path = require('node:path')
const root = path.resolve(__dirname, '..')
const source = fs.readFileSync(path.join(root, 'scores-api/src/modules/matches/score.engine.js'), 'utf8')
const output = '// Generated by scripts/sync-score-engine.cjs.\n' + source.replace('module.exports = {', 'export {')
const folder = path.join(root, 'scores-app/src/generated')
fs.mkdirSync(folder, { recursive: true })
fs.writeFileSync(path.join(folder, 'scoreEngine.js'), output)
