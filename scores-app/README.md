# Frontend · Tenis Club Unión

La documentación completa de instalación, seguridad y despliegue está en [`../README.md`](../README.md).

Comandos principales:

```text
npm install
npm run dev
npm run build
```

La URL de la API se configura con `VITE_API_URL`. En el contenedor de producción se usa `/api`.
